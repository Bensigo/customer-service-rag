# Secrets — never commit
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Node
node_modules/
dist/
coverage/

# OS / editor
.DS_Store
.idea/
.vscode/

# Local data (SQLite chunk store, ingested documents) — but keep the
# checked-in sample corpus and golden eval dataset (#14).
data/*
!data/samples/
!data/eval/
data/eval/*
!data/eval/golden.jsonl

# Logs
*.log
